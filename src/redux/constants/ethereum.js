/** @module redux_ethereum_constants */

/**
 * @enum {String} EthereumActionTypes -- Reducer State Constants
 * Used specifically within the application reducer's switch statement to determine what updates to state occur 
 */
 export const ETHEREUM_ACTION_TYPES = {
    UPDATE_ETHEREUM_INSTANCE_STATE_BY_KEYCHAIN: "UPDATE_ETHEREUM_INSTANCE_STATE_BY_KEYCHAIN",
};